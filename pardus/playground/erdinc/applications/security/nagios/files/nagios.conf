# Pardus Linux Nagios configuration file for Apache Web Server

ScriptAlias /nagios/cgi-bin /usr/nagios/sbin

<Directory "/usr/nagios/sbin">
#  SSLRequireSSL
   AllowOverride AuthConfig
   Options ExecCGI
   Order allow,deny
   Allow from all
#   Order deny,allow
#   Deny from all
#   Allow from 127.0.0.1
#   AuthName "Nagios Access"
#   AuthType Basic
#   AuthUserFile /etc/nagios/htpasswd.users
#   Require valid-user
</Directory>

Alias /nagios "/usr/nagios/share"

<Directory "/usr/nagios/share">
#  SSLRequireSSL
   AllowOverride AuthConfig
   AllowOverride None
   Order allow,deny
   Allow from all
#   Order deny,allow
#   Deny from all
#   Allow from 127.0.0.1
#   AuthName "Nagios Access"
#   AuthType Basic
#   AuthUserFile /etc/nagios/htpasswd.users
#   Require valid-user
</Directory>
