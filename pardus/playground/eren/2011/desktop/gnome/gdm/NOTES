gdm user and group should be added to use gdm. Give these commands 
before 
you install it with root privileges;

groupadd gdm
useradd -s /bin/false -u 210 -g gdm gdm

After the gdm installation, enter;
chown root:gdm /var/lib/gdm
chown root:gdm /var/lib/log/gdm
