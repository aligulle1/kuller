diff -Nur slim-1.3.0.orig/app.cpp slim-1.3.0/app.cpp
--- slim-1.3.0.orig/app.cpp	2008-06-08 20:34:13.000000000 +0300
+++ slim-1.3.0/app.cpp	2008-06-08 22:32:47.000000000 +0300
@@ -472,6 +472,9 @@
 void App::Login() {
     struct passwd *pw;
     pid_t pid;
+    // Locale entry variables for Pardus
+    ifstream localefile ("/etc/mudur/locale");
+    char locale[20];
 
 #ifdef USE_PAM
     try{
@@ -547,6 +550,16 @@
         child_env[n++]=StrConcat("DISPLAY=", DisplayName);
         child_env[n++]=StrConcat("MAIL=", maildir.c_str());
         child_env[n++]=StrConcat("XAUTHORITY=", xauthority.c_str());
+        // Read Pardus' locale entry from mudur file and append it.
+        // If we don't append, the desktop environment we login into doesn't support UTF-8
+        if (localefile.is_open())
+        {
+            localefile.getline(locale, 20);
+            localefile.close();
+            child_env[n++]=StrConcat("LC_ALL=", locale);
+            child_env[n++]=StrConcat("LANG=", locale);
+        }
+
         child_env[n++]=0;
 #endif
 
