Xorg TODO List
==============

 + Split server package
 + Split driver packages
   + xorg-input
   + xorg-video

 + Find a consistent package naming scheme:
   + Servers:
     + xorg-server-common
     + xorg-server
     + xorg-server-xvfb
     + xorg-server-xdmx
     + xorg-server-xephyr
   + Video drivers:
     + xorg-video-fbdev
     + xorg-video-vesa
     ...
   + Input drivers:
     + xorg-input-evdev
     + xorg-input-synaptics
     ...

 + Use /etc/X11/fontpath.d instead of static font paths
 + Make ModeDebug enabled by default (Only for development/test packages)

 + New deps to be packaged:
   + mesa
   + libpciaccess
   + nouveau drivers

 + Add appropriate tags for updates
   + drivers replace xorg-{input,video}
   + mesa conflicts with old xorg-server

 + Mark deprecated packages as _obsolete_
   + libXevie
   + libXfontcache
   + libXprintAppUtil
   + libXprintUtil
   + libXTrap
   + libXxf86misc

 \ xinit/xdm scripts
   - respawn support in start-dm.py
