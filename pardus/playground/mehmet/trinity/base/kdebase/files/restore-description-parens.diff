Index: kdebase/kicker/kicker/ui/service_mnu.cpp
===================================================================
--- kdebase.orig/kicker/kicker/ui/service_mnu.cpp
+++ kdebase/kicker/kicker/ui/service_mnu.cpp
@@ -416,7 +416,7 @@ void PanelServiceMenu::insertMenuItem(KS
             {
                 if (comment.find(serviceName, 0, true) == -1)
                 {
-                    serviceName = i18n("Entries in K-menu: %1 app name, %2 description", "%1 - %2").arg(serviceName, comment);
+                    serviceName = i18n("Entries in K-menu: %1 app name, %2 description", "%1 (%2)").arg(serviceName, comment);
                 }
                 else
                 {
