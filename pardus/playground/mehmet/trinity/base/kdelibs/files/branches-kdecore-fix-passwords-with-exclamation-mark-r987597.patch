Index: kdecore/kstringhandler.cpp
===================================================================
--- kdecore.orig/kstringhandler.cpp
+++ kdecore/kstringhandler.cpp
@@ -558,7 +558,7 @@ TQString KStringHandler::obscure( const
   TQString result;
   const TQChar *unicode = str.unicode();
   for ( uint i = 0; i < str.length(); ++i )
-    result += ( unicode[ i ].unicode() < 0x21 ) ? unicode[ i ] :
+    result += ( unicode[ i ].unicode() <= 0x21 ) ? unicode[ i ] :
         TQChar( 0x1001F - unicode[ i ].unicode() );
 
   return result;
