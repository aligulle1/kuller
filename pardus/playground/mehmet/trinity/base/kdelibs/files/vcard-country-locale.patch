Index: kabc/address.cpp
===================================================================
--- kabc.orig/address.cpp
+++ kabc/address.cpp
@@ -29,11 +29,21 @@
 
 #include <tqfile.h>
 
+#include <unicode/unistr.h>
+
 using namespace KABC;
 
 TQMap<TQString, TQString> *Address::mISOMap = 0;
 static KStaticDeleter< TQMap<TQString, TQString> > isoMapDeleter;
 
+QString toUpper(const QString& value)
+{
+  UnicodeString us(FALSE /* Not NUL-terminated */, (const UChar *)value.unicode(), (int32_t)value.length());
+  us = us.toUpper();
+
+  return QString((const QChar*)us.getBuffer(),us.length());
+}
+
 Address::Address() :
   mEmpty( true ), mType( 0 )
 {
@@ -373,7 +383,7 @@ TQString Address::formattedAddress( cons
     entry.setGroup( "KCM Locale" );
     TQString cpos = entry.readEntry( "AddressCountryPosition" );
     if ( "BELOW" == cpos || cpos.isEmpty() ) {
-      ret = ret + "\n\n" + country().upper();
+      ret = ret + "\n\n" + toUpper(country());
     } else if ( "below" == cpos ) {
       ret = ret + "\n\n" + country();
     } else if ( "ABOVE" == cpos ) {
Index: kabc/Makefile.am
===================================================================
--- kabc.orig/Makefile.am
+++ kabc/Makefile.am
@@ -22,7 +22,7 @@ CLEANFILES = addressee.h addressee.cpp f
 lib_LTLIBRARIES = libkabc.la
 libkabc_la_LDFLAGS = $(KDE_RPATH) $(all_libraries) -version-info 3:0:2 -no-undefined
 libkabc_la_LIBADD = vcard/libvcard.la vcardparser/libvcards.la $(LIB_KIO) \
-                    $(top_builddir)/kresources/libkresources.la $(LIB_QT) $(top_builddir)/dcop/libDCOP.la $(LIB_KDEUI) $(LIB_KDECORE)
+                    $(top_builddir)/kresources/libkresources.la -licuuc $(LIB_QT) $(top_builddir)/dcop/libDCOP.la $(LIB_KDEUI) $(LIB_KDECORE)
 libkabc_la_COMPILE_FIRST = addressee.h
 
 libkabc_la_SOURCES = \
