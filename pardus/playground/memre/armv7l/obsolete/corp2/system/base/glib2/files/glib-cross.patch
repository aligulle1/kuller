--- configure.orig	2010-02-08 23:29:02.000000000 +0200
+++ configure	2010-02-08 23:32:53.000000000 +0200
@@ -474,7 +474,7 @@
     ' >$as_me.lineno &&
   chmod +x "$as_me.lineno" ||
     { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2
-   { (exit 1); exit 1; }; }
+    }
 
   # Don't try to exec as it changes $[0], causing all sort of problems
   # (the dirname of $[0] is not the place where we might find the
@@ -1213,7 +1213,7 @@
     # Reject names that are not valid shell variable names.
     expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
       { $as_echo "$as_me: error: invalid feature name: $ac_useropt" >&2
-   { (exit 1); exit 1; }; }
+    }
     ac_useropt_orig=$ac_useropt
     ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
     case $ac_user_opts in
@@ -1240,7 +1240,7 @@
     # Reject names that are not valid shell variable names.
     expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
       { $as_echo "$as_me: error: invalid feature name: $ac_useropt" >&2
-   { (exit 1); exit 1; }; }
+    }
     ac_useropt_orig=$ac_useropt
     ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
     case $ac_user_opts in
@@ -1445,7 +1445,7 @@
     # Reject names that are not valid shell variable names.
     expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
       { $as_echo "$as_me: error: invalid package name: $ac_useropt" >&2
-   { (exit 1); exit 1; }; }
+    }
     ac_useropt_orig=$ac_useropt
     ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
     case $ac_user_opts in
@@ -1462,7 +1462,7 @@
     # Reject names that are not valid shell variable names.
     expr "x$ac_useropt" : ".*[^-+._$as_cr_alnum]" >/dev/null &&
       { $as_echo "$as_me: error: invalid package name: $ac_useropt" >&2
-   { (exit 1); exit 1; }; }
+    }
     ac_useropt_orig=$ac_useropt
     ac_useropt=`$as_echo "$ac_useropt" | sed 's/[-+.]/_/g'`
     case $ac_user_opts in
@@ -1494,7 +1494,7 @@
 
   -*) { $as_echo "$as_me: error: unrecognized option: $ac_option
 Try \`$0 --help' for more information." >&2
-   { (exit 1); exit 1; }; }
+    }
     ;;
 
   *=*)
@@ -1502,7 +1502,7 @@
     # Reject names that are not valid shell variable names.
     expr "x$ac_envvar" : ".*[^_$as_cr_alnum]" >/dev/null &&
       { $as_echo "$as_me: error: invalid variable name: $ac_envvar" >&2
-   { (exit 1); exit 1; }; }
+    }
     eval $ac_envvar=\$ac_optarg
     export $ac_envvar ;;
 
@@ -1520,14 +1520,14 @@
 if test -n "$ac_prev"; then
   ac_option=--`echo $ac_prev | sed 's/_/-/g'`
   { $as_echo "$as_me: error: missing argument to $ac_option" >&2
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 if test -n "$ac_unrecognized_opts"; then
   case $enable_option_checking in
     no) ;;
     fatal) { $as_echo "$as_me: error: unrecognized options: $ac_unrecognized_opts" >&2
-   { (exit 1); exit 1; }; } ;;
+    } ;;
     *)     $as_echo "$as_me: WARNING: unrecognized options: $ac_unrecognized_opts" >&2 ;;
   esac
 fi
@@ -1551,7 +1551,7 @@
     NONE | '' ) case $ac_var in *prefix ) continue;; esac;;
   esac
   { $as_echo "$as_me: error: expected an absolute directory name for --$ac_var: $ac_val" >&2
-   { (exit 1); exit 1; }; }
+    }
 done
 
 # There might be people who depend on the old broken behavior: `$host'
@@ -1582,10 +1582,10 @@
 ac_ls_di=`ls -di .` &&
 ac_pwd_ls_di=`cd "$ac_pwd" && ls -di .` ||
   { $as_echo "$as_me: error: working directory cannot be determined" >&2
-   { (exit 1); exit 1; }; }
+    }
 test "X$ac_ls_di" = "X$ac_pwd_ls_di" ||
   { $as_echo "$as_me: error: pwd does not report name of working directory" >&2
-   { (exit 1); exit 1; }; }
+    }
 
 
 # Find the source files, if location was not specified.
@@ -1625,12 +1625,12 @@
 if test ! -r "$srcdir/$ac_unique_file"; then
   test "$ac_srcdir_defaulted" = yes && srcdir="$ac_confdir or .."
   { $as_echo "$as_me: error: cannot find sources ($ac_unique_file) in $srcdir" >&2
-   { (exit 1); exit 1; }; }
+    }
 fi
 ac_msg="sources are in $srcdir, but \`cd $srcdir' does not work"
 ac_abs_confdir=`(
 	cd "$srcdir" && test -r "./$ac_unique_file" || { $as_echo "$as_me: error: $ac_msg" >&2
-   { (exit 1); exit 1; }; }
+    }
 	pwd)`
 # When building in place, set srcdir=.
 if test "$ac_abs_confdir" = "$ac_pwd"; then
@@ -2219,7 +2219,7 @@
 $as_echo "$as_me: error: changes in the environment can compromise the build" >&2;}
   { { $as_echo "$as_me:$LINENO: error: run \`make distclean' and/or \`rm $cache_file' and start over" >&5
 $as_echo "$as_me: error: run \`make distclean' and/or \`rm $cache_file' and start over" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 
@@ -2280,7 +2280,7 @@
 if test -z "$ac_aux_dir"; then
   { { $as_echo "$as_me:$LINENO: error: cannot find install-sh or install.sh in \"$srcdir\" \"$srcdir/..\" \"$srcdir/../..\"" >&5
 $as_echo "$as_me: error: cannot find install-sh or install.sh in \"$srcdir\" \"$srcdir/..\" \"$srcdir/../..\"" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 # These three variables are undocumented and unsupported,
@@ -2398,13 +2398,13 @@
   *[\\\"\#\$\&\'\`$am_lf]*)
     { { $as_echo "$as_me:$LINENO: error: unsafe absolute working directory name" >&5
 $as_echo "$as_me: error: unsafe absolute working directory name" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
 esac
 case $srcdir in
   *[\\\"\#\$\&\'\`$am_lf\ \	]*)
     { { $as_echo "$as_me:$LINENO: error: unsafe srcdir value: \`$srcdir'" >&5
 $as_echo "$as_me: error: unsafe srcdir value: \`$srcdir'" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
 esac
 
 # Do `set' in a subshell so we don't clobber the current shell's
@@ -2430,7 +2430,7 @@
 alias in your environment" >&5
 $as_echo "$as_me: error: ls -t appears to fail.  Make sure there is not a broken
 alias in your environment" >&2;}
-   { (exit 1); exit 1; }; }
+    }
    fi
 
    test "$2" = conftest.file
@@ -2443,7 +2443,7 @@
 Check your system clock" >&5
 $as_echo "$as_me: error: newly created file is older than distributed files!
 Check your system clock" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 { $as_echo "$as_me:$LINENO: result: yes" >&5
 $as_echo "yes" >&6; }
@@ -2724,7 +2724,7 @@
   if test -f $srcdir/config.status; then
     { { $as_echo "$as_me:$LINENO: error: source directory already configured; run \"make distclean\" there first" >&5
 $as_echo "$as_me: error: source directory already configured; run \"make distclean\" there first" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 fi
 
@@ -2872,7 +2872,7 @@
 $SHELL "$ac_aux_dir/config.sub" sun4 >/dev/null 2>&1 ||
   { { $as_echo "$as_me:$LINENO: error: cannot run $SHELL $ac_aux_dir/config.sub" >&5
 $as_echo "$as_me: error: cannot run $SHELL $ac_aux_dir/config.sub" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 
 { $as_echo "$as_me:$LINENO: checking build system type" >&5
 $as_echo_n "checking build system type... " >&6; }
@@ -2885,11 +2885,11 @@
 test "x$ac_build_alias" = x &&
   { { $as_echo "$as_me:$LINENO: error: cannot guess build type; you must specify one" >&5
 $as_echo "$as_me: error: cannot guess build type; you must specify one" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 ac_cv_build=`$SHELL "$ac_aux_dir/config.sub" $ac_build_alias` ||
   { { $as_echo "$as_me:$LINENO: error: $SHELL $ac_aux_dir/config.sub $ac_build_alias failed" >&5
 $as_echo "$as_me: error: $SHELL $ac_aux_dir/config.sub $ac_build_alias failed" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 
 fi
 { $as_echo "$as_me:$LINENO: result: $ac_cv_build" >&5
@@ -2898,7 +2898,7 @@
 *-*-*) ;;
 *) { { $as_echo "$as_me:$LINENO: error: invalid value of canonical build" >&5
 $as_echo "$as_me: error: invalid value of canonical build" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
 esac
 build=$ac_cv_build
 ac_save_IFS=$IFS; IFS='-'
@@ -2925,7 +2925,7 @@
   ac_cv_host=`$SHELL "$ac_aux_dir/config.sub" $host_alias` ||
     { { $as_echo "$as_me:$LINENO: error: $SHELL $ac_aux_dir/config.sub $host_alias failed" >&5
 $as_echo "$as_me: error: $SHELL $ac_aux_dir/config.sub $host_alias failed" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 fi
@@ -2935,7 +2935,7 @@
 *-*-*) ;;
 *) { { $as_echo "$as_me:$LINENO: error: invalid value of canonical host" >&5
 $as_echo "$as_me: error: invalid value of canonical host" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
 esac
 host=$ac_cv_host
 ac_save_IFS=$IFS; IFS='-'
@@ -3374,7 +3374,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: no acceptable C compiler found in \$PATH
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 
 # Provide some information about the compiler.
 $as_echo "$as_me:$LINENO: checking for C compiler version" >&5
@@ -3546,7 +3546,7 @@
 $as_echo "$as_me: error: cannot run C compiled programs.
 If you meant to cross compile, use \`--host'.
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
     fi
   fi
 fi
@@ -3595,7 +3595,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot compute suffix of executables: cannot compile and link
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 fi
 
 rm -f conftest$ac_cv_exeext
@@ -3655,7 +3655,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot compute suffix of object files: cannot compile
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 fi
 
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
@@ -4358,7 +4358,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: C preprocessor \"$CPP\" fails sanity check
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 fi
 
 ac_ext=c
@@ -4546,7 +4546,7 @@
   if test "$WINDRES" = no; then
     { { $as_echo "$as_me:$LINENO: error: *** Could not find an implementation of windres in your PATH." >&5
 $as_echo "$as_me: error: *** Could not find an implementation of windres in your PATH." >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
   if test -n "$ac_tool_prefix"; then
   # Extract the first word of "${ac_tool_prefix}nm", so it can be a program name with args.
@@ -4643,7 +4643,7 @@
   if test "$NM" = no; then
     { { $as_echo "$as_me:$LINENO: error: *** Could not find an implementation of nm in your PATH." >&5
 $as_echo "$as_me: error: *** Could not find an implementation of nm in your PATH." >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
   if test -n "$ac_tool_prefix"; then
   # Extract the first word of "${ac_tool_prefix}ranlib", so it can be a program name with args.
@@ -5209,7 +5209,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: no acceptable C compiler found in \$PATH
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 
 # Provide some information about the compiler.
 $as_echo "$as_me:$LINENO: checking for C compiler version" >&5
@@ -5942,7 +5942,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: C preprocessor \"$CPP\" fails sanity check
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 fi
 
 ac_ext=c
@@ -6920,7 +6920,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: no acceptable C compiler found in \$PATH
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 
 # Provide some information about the compiler.
 $as_echo "$as_me:$LINENO: checking for C compiler version" >&5
@@ -8059,7 +8059,7 @@
 if test x$PKG_CONFIG = xno ; then
   { { $as_echo "$as_me:$LINENO: error: *** pkg-config not found. See http://www.freedesktop.org/software/pkgconfig/" >&5
 $as_echo "$as_me: error: *** pkg-config not found. See http://www.freedesktop.org/software/pkgconfig/" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 if $PKG_CONFIG --atleast-pkgconfig-version 0.16 ; then
@@ -8067,7 +8067,7 @@
 else
   { { $as_echo "$as_me:$LINENO: error: *** pkg-config too old; version 0.16 or better required." >&5
 $as_echo "$as_me: error: *** pkg-config too old; version 0.16 or better required." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 if test "x$enable_debug" = "xyes"; then
@@ -8344,7 +8344,7 @@
 else
   { { $as_echo "$as_me:$LINENO: error: too old" >&5
 $as_echo "$as_me: error: too old" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
       am_display_PYTHON=$PYTHON
@@ -9039,7 +9039,7 @@
   if test "x$found_iconv" = "xno" ; then
      { { $as_echo "$as_me:$LINENO: error: *** No iconv() implementation found in C library or libiconv" >&5
 $as_echo "$as_me: error: *** No iconv() implementation found in C library or libiconv" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 fi
 
@@ -9096,7 +9096,7 @@
   if test -z "$ac_cv_path_GREP"; then
     { { $as_echo "$as_me:$LINENO: error: no acceptable grep could be found in $PATH$PATH_SEPARATOR/usr/xpg4/bin" >&5
 $as_echo "$as_me: error: no acceptable grep could be found in $PATH$PATH_SEPARATOR/usr/xpg4/bin" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 else
   ac_cv_path_GREP=$GREP
@@ -9164,7 +9164,7 @@
   if test -z "$ac_cv_path_EGREP"; then
     { { $as_echo "$as_me:$LINENO: error: no acceptable egrep could be found in $PATH$PATH_SEPARATOR/usr/xpg4/bin" >&5
 $as_echo "$as_me: error: no acceptable egrep could be found in $PATH$PATH_SEPARATOR/usr/xpg4/bin" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 else
   ac_cv_path_EGREP=$EGREP
@@ -9237,7 +9237,7 @@
   ;;
   *) { { $as_echo "$as_me:$LINENO: error: Value given to --enable-iconv-cache must be one of yes, no or auto" >&5
 $as_echo "$as_me: error: Value given to --enable-iconv-cache must be one of yes, no or auto" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   ;;
 esac
 
@@ -11113,7 +11113,7 @@
 *** You must have either have gettext support in your C library, or use the
 *** GNU gettext library. (http://www.gnu.org/software/gettext/gettext.html
 " >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 LIBS="$INTLLIBS $LIBS"
@@ -11593,7 +11593,7 @@
   if test -z "$ac_cv_path_SED"; then
     { { $as_echo "$as_me:$LINENO: error: no acceptable sed could be found in \$PATH" >&5
 $as_echo "$as_me: error: no acceptable sed could be found in \$PATH" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 else
   ac_cv_path_SED=$SED
@@ -11674,7 +11674,7 @@
   if test -z "$ac_cv_path_FGREP"; then
     { { $as_echo "$as_me:$LINENO: error: no acceptable fgrep could be found in $PATH$PATH_SEPARATOR/usr/xpg4/bin" >&5
 $as_echo "$as_me: error: no acceptable fgrep could be found in $PATH$PATH_SEPARATOR/usr/xpg4/bin" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 else
   ac_cv_path_FGREP=$FGREP
@@ -11792,7 +11792,7 @@
 fi
 test -z "$LD" && { { $as_echo "$as_me:$LINENO: error: no acceptable ld found in \$PATH" >&5
 $as_echo "$as_me: error: no acceptable ld found in \$PATH" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 { $as_echo "$as_me:$LINENO: checking if the linker ($LD) is GNU ld" >&5
 $as_echo_n "checking if the linker ($LD) is GNU ld... " >&6; }
 if test "${lt_cv_prog_gnu_ld+set}" = set; then
@@ -19087,7 +19087,7 @@
 fi
 test -z "$LD" && { { $as_echo "$as_me:$LINENO: error: no acceptable ld found in \$PATH" >&5
 $as_echo "$as_me: error: no acceptable ld found in \$PATH" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 { $as_echo "$as_me:$LINENO: checking if the linker ($LD) is GNU ld" >&5
 $as_echo_n "checking if the linker ($LD) is GNU ld... " >&6; }
 if test "${lt_cv_prog_gnu_ld+set}" = set; then
@@ -22039,7 +22039,7 @@
   chmod +x "$as_me.lineno" ||
     { { $as_echo "$as_me:$LINENO: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&5
 $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 
   # Don't try to exec as it changes $[0], causing all sort of problems
   # (the dirname of $[0] is not the place where we might find the
@@ -22185,13 +22185,13 @@
 Try \`$0 --help' for more information." >&5
 $as_echo "$as_me: error: unrecognized option: $1
 Try \`$0 --help' for more information." >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 
     *) { { $as_echo "$as_me:$LINENO: error: unrecognized argument: $1
 Try \`$0 --help' for more information." >&5
 $as_echo "$as_me: error: unrecognized argument: $1
 Try \`$0 --help' for more information." >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
   esac
   shift
 done
@@ -23400,7 +23400,7 @@
   if test x$enable_static = xyes -a x$enable_shared = xyes; then
     { { $as_echo "$as_me:$LINENO: error: Can not build both shared and static at the same time on Windows." >&5
 $as_echo "$as_me: error: Can not build both shared and static at the same time on Windows." >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
   if test x$enable_static = xyes; then
     glib_win32_static_compilation=yes
@@ -23430,7 +23430,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -23480,7 +23480,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -27126,7 +27126,7 @@
 *** GLib requires a 64 bit type. You might want to consider
 *** using the GNU C compiler.
 " >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 if test x$glib_native_win32 != xyes && test x$ac_cv_sizeof_long_long = x8; then
@@ -27423,7 +27423,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -28472,7 +28472,7 @@
  presetting ac_cv_c_bigendian=no (or yes) will help" >&5
 $as_echo "$as_me: error: unknown endianness
  presetting ac_cv_c_bigendian=no (or yes) will help" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
  esac
 
 if test x$ac_cv_c_bigendian = xuniversal ; then
@@ -31481,7 +31481,7 @@
       ;;
   *)  { { $as_echo "$as_me:$LINENO: error: No type matching size_t in size" >&5
 $as_echo "$as_me: error: No type matching size_t in size" >&2;}
-   { (exit 1); exit 1; }; }
+    }
       ;;
 esac
 
@@ -33438,7 +33438,7 @@
 if $glib_failed ; then
   { { $as_echo "$as_me:$LINENO: error: Could not determine values for AF_INET* constants" >&5
 $as_echo "$as_me: error: Could not determine values for AF_INET* constants" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 glib_failed=false
@@ -34432,7 +34432,7 @@
 if $glib_failed ; then
   { { $as_echo "$as_me:$LINENO: error: Could not determine values for MSG_* constants" >&5
 $as_echo "$as_me: error: Could not determine values for MSG_* constants" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 
@@ -35224,7 +35224,7 @@
 else
    { { $as_echo "$as_me:$LINENO: error: res_query not found" >&5
 $as_echo "$as_me: error: res_query not found" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 fi
@@ -35388,7 +35388,7 @@
 $as_echo "unknown" >&6; }
       { { $as_echo "$as_me:$LINENO: error: unable to determine number of arguments to statfs()" >&5
 $as_echo "$as_me: error: unable to determine number of arguments to statfs()" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext
@@ -35430,7 +35430,7 @@
 $as_echo "$as_me: error:
 *** Your C library's printf doesn't appear to have the features that
 *** GLib needs, but you specified --enable-included-printf=no." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 enable_included_printf=$need_included_printf
@@ -36334,7 +36334,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -36402,7 +36402,7 @@
   if test "x${glib_cv_working_bcopy+set}" != "xset" ; then
   { { $as_echo "$as_me:$LINENO: error: glib_cv_working_bcopy must be set in cache file when cross-compiling." >&5
 $as_echo "$as_me: error: glib_cv_working_bcopy must be set in cache file when cross-compiling." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
   if test "$glib_cv_working_bcopy" = "yes"; then
@@ -37869,7 +37869,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -37932,7 +37932,7 @@
         if test "x${glib_cv_uscore+set}" != "xset" ; then
   { { $as_echo "$as_me:$LINENO: error: glib_cv_uscore must be set in cache file when cross-compiling." >&5
 $as_echo "$as_me: error: glib_cv_uscore must be set in cache file when cross-compiling." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 	if test "x$glib_cv_uscore" = "xyes"; then
@@ -38056,7 +38056,7 @@
 if test "x$glib_gmodule_suffix" = x; then
 	{ { $as_echo "$as_me:$LINENO: error: Cannot determine shared library suffix from libtool" >&5
 $as_echo "$as_me: error: Cannot determine shared library suffix from libtool" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 
@@ -40281,7 +40281,7 @@
 		if test "x$G_THREAD_LIBS" = xerror; then
 		  { { $as_echo "$as_me:$LINENO: error: $LIBS_NOT_FOUND_1$have_threads$LIBS_NOT_FOUND_2" >&5
 $as_echo "$as_me: error: $LIBS_NOT_FOUND_1$have_threads$LIBS_NOT_FOUND_2" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 		fi
 		LIBS="$glib_save_LIBS"
                 ;;
@@ -40435,7 +40435,7 @@
 if test "x$G_THREAD_LIBS" = xerror; then
         { { $as_echo "$as_me:$LINENO: error: $LIBS_NOT_FOUND_1$have_threads$LIBS_NOT_FOUND_2" >&5
 $as_echo "$as_me: error: $LIBS_NOT_FOUND_1$have_threads$LIBS_NOT_FOUND_2" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 case $host in
@@ -40573,7 +40573,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -40637,7 +40637,7 @@
 		if test "x${ac_cv_func_posix_getpwuid_r+set}" != "xset" ; then
   { { $as_echo "$as_me:$LINENO: error: ac_cv_func_posix_getpwuid_r must be set in cache file when cross-compiling." >&5
 $as_echo "$as_me: error: ac_cv_func_posix_getpwuid_r must be set in cache file when cross-compiling." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 		if test "$ac_cv_func_posix_getpwuid_r" = yes; then
@@ -40708,7 +40708,7 @@
 			if test "x${ac_cv_func_nonposix_getpwuid_r+set}" != "xset" ; then
   { { $as_echo "$as_me:$LINENO: error: ac_cv_func_nonposix_getpwuid_r must be set in cache file when cross-compiling." >&5
 $as_echo "$as_me: error: ac_cv_func_nonposix_getpwuid_r must be set in cache file when cross-compiling." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 			if test "$ac_cv_func_nonposix_getpwuid_r" = yes; then
@@ -40733,7 +40733,7 @@
 See \`config.log' for more details." >&5
 $as_echo "$as_me: error: cannot run test program while cross compiling
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
   cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -40797,7 +40797,7 @@
 		if test "x${ac_cv_func_posix_getgrgid_r+set}" != "xset" ; then
   { { $as_echo "$as_me:$LINENO: error: ac_cv_func_posix_getgrgid_r must be set in cache file when cross-compiling." >&5
 $as_echo "$as_me: error: ac_cv_func_posix_getgrgid_r must be set in cache file when cross-compiling." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 		if test "$ac_cv_func_posix_getgrgid_r" = yes; then
@@ -40868,7 +40868,7 @@
 			if test "x${ac_cv_func_nonposix_getgrgid_r+set}" != "xset" ; then
   { { $as_echo "$as_me:$LINENO: error: ac_cv_func_nonposix_getgrgid_r must be set in cache file when cross-compiling." >&5
 $as_echo "$as_me: error: ac_cv_func_nonposix_getgrgid_r must be set in cache file when cross-compiling." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 			if test "$ac_cv_func_nonposix_getgrgid_r" = yes; then
@@ -41141,7 +41141,7 @@
 ?*) glib_cv_sizeof_system_thread=$ac_lo;;
 '') { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof (pthread_t), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof (pthread_t), 77" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 esac
 else
   cat >conftest.$ac_ext <<_ACEOF
@@ -41216,7 +41216,7 @@
 ( exit $ac_status )
 { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof (pthread_t), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof (pthread_t), 77" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 rm -rf conftest.dSYM
 rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext conftest.$ac_objext conftest.$ac_ext
@@ -41756,7 +41756,7 @@
 ?*) glib_cv_sizeof_system_thread=$ac_lo;;
 '') { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof (struct _GThreadData *), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof (struct _GThreadData *), 77" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 esac
 else
   cat >conftest.$ac_ext <<_ACEOF
@@ -41830,7 +41830,7 @@
 ( exit $ac_status )
 { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof (struct _GThreadData *), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof (struct _GThreadData *), 77" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 rm -rf conftest.dSYM
 rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext conftest.$ac_objext conftest.$ac_ext
@@ -42116,7 +42116,7 @@
 ?*) glib_cv_sizeof_system_thread=$ac_lo;;
 '') { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof (void *), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof (void *), 77" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 esac
 else
   cat >conftest.$ac_ext <<_ACEOF
@@ -42190,7 +42190,7 @@
 ( exit $ac_status )
 { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof (void *), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof (void *), 77" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 rm -rf conftest.dSYM
 rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext conftest.$ac_objext conftest.$ac_ext
@@ -42496,7 +42496,7 @@
 ?*) glib_cv_sizeof_gmutex=$ac_lo;;
 '') { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof ($mutex_default_type), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof ($mutex_default_type), 77" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 esac
 else
   cat >conftest.$ac_ext <<_ACEOF
@@ -42571,7 +42571,7 @@
 ( exit $ac_status )
 { { $as_echo "$as_me:$LINENO: error: cannot compute sizeof ($mutex_default_type), 77" >&5
 $as_echo "$as_me: error: cannot compute sizeof ($mutex_default_type), 77" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 rm -rf conftest.dSYM
 rm -f core *.core core.conftest.* gmon.out bb.out conftest$ac_exeext conftest.$ac_objext conftest.$ac_ext
@@ -45127,7 +45127,7 @@
   if $glib_failed ; then
     { { $as_echo "$as_me:$LINENO: error: Could not determine values for POLL* constants" >&5
 $as_echo "$as_me: error: Could not determine values for POLL* constants" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 else
   glib_cv_value_POLLIN=1
@@ -45223,7 +45223,7 @@
   no)  enable_regex=false ;;
   *) { { $as_echo "$as_me:$LINENO: error: bad value ${enableval} for --enable-regex" >&5
 $as_echo "$as_me: error: bad value ${enableval} for --enable-regex" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 esac
 else
   enable_regex=true
@@ -45456,7 +45456,7 @@
 and PCRE_LIBS to avoid the need to call pkg-config.
 See the pkg-config man page for more details.
 " >&2;}
-   { (exit 1); exit 1; }; }
+    }
 elif test $pkg_failed = untried; then
 	{ { $as_echo "$as_me:$LINENO: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
@@ -45480,7 +45480,7 @@
 
 To get pkg-config, see <http://pkg-config.freedesktop.org/>.
 See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
 else
 	PCRE_CFLAGS=$pkg_cv_PCRE_CFLAGS
 	PCRE_LIBS=$pkg_cv_PCRE_LIBS
@@ -45563,7 +45563,7 @@
     if test "$glib_cv_pcre_has_unicode" = "no"; then
       { { $as_echo "$as_me:$LINENO: error: *** The system-supplied PCRE does not support Unicode properties or UTF-8." >&5
 $as_echo "$as_me: error: *** The system-supplied PCRE does not support Unicode properties or UTF-8." >&2;}
-   { (exit 1); exit 1; }; }
+    }
     fi
 
 
@@ -45762,7 +45762,7 @@
   if test x$GLIB_GENMARSHAL = xno; then
     { { $as_echo "$as_me:$LINENO: error: Could not find a glib-genmarshal in your PATH" >&5
 $as_echo "$as_me: error: Could not find a glib-genmarshal in your PATH" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 fi
 
@@ -45926,7 +45926,7 @@
 else
   { { $as_echo "$as_me:$LINENO: error: You need to have gtk-doc >= 1.11 installed to build $PACKAGE_NAME" >&5
 $as_echo "$as_me: error: You need to have gtk-doc >= 1.11 installed to build $PACKAGE_NAME" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
   fi
 
@@ -46299,126 +46299,126 @@
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"MAINTAINER_MODE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${AMDEP_TRUE}" && test -z "${AMDEP_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"AMDEP\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"AMDEP\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${am__fastdepCC_TRUE}" && test -z "${am__fastdepCC_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"am__fastdepCC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"am__fastdepCC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${OS_WIN32_TRUE}" && test -z "${OS_WIN32_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"OS_WIN32\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"OS_WIN32\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${OS_WIN32_X64_TRUE}" && test -z "${OS_WIN32_X64_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"OS_WIN32_X64\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"OS_WIN32_X64\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${OS_UNIX_TRUE}" && test -z "${OS_UNIX_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"OS_UNIX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"OS_UNIX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${OS_LINUX_TRUE}" && test -z "${OS_LINUX_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"OS_LINUX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"OS_LINUX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${OS_CARBON_TRUE}" && test -z "${OS_CARBON_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"OS_CARBON\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"OS_CARBON\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${MS_LIB_AVAILABLE_TRUE}" && test -z "${MS_LIB_AVAILABLE_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"MS_LIB_AVAILABLE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"MS_LIB_AVAILABLE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_GLIB_RUNTIME_LIBDIR_TRUE}" && test -z "${HAVE_GLIB_RUNTIME_LIBDIR_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_GLIB_RUNTIME_LIBDIR\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_GLIB_RUNTIME_LIBDIR\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${am__fastdepCC_TRUE}" && test -z "${am__fastdepCC_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"am__fastdepCC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"am__fastdepCC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${am__fastdepCXX_TRUE}" && test -z "${am__fastdepCXX_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"am__fastdepCXX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"am__fastdepCXX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_CXX_TRUE}" && test -z "${HAVE_CXX_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_CXX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_CXX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${am__fastdepCC_TRUE}" && test -z "${am__fastdepCC_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"am__fastdepCC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"am__fastdepCC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${am__fastdepCXX_TRUE}" && test -z "${am__fastdepCXX_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"am__fastdepCXX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"am__fastdepCXX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${OS_WIN32_AND_DLL_COMPILATION_TRUE}" && test -z "${OS_WIN32_AND_DLL_COMPILATION_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"OS_WIN32_AND_DLL_COMPILATION\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"OS_WIN32_AND_DLL_COMPILATION\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_GNUC_VISIBILITY_TRUE}" && test -z "${HAVE_GNUC_VISIBILITY_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_GNUC_VISIBILITY\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_GNUC_VISIBILITY\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_SUNSTUDIO_VISIBILITY_TRUE}" && test -z "${HAVE_SUNSTUDIO_VISIBILITY_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_SUNSTUDIO_VISIBILITY\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_SUNSTUDIO_VISIBILITY\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 if test -z "${HAVE_GOOD_PRINTF_TRUE}" && test -z "${HAVE_GOOD_PRINTF_FALSE}"; then
@@ -46426,112 +46426,112 @@
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_GOOD_PRINTF\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_INOTIFY_TRUE}" && test -z "${HAVE_INOTIFY_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_INOTIFY\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_INOTIFY\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_FEN_TRUE}" && test -z "${HAVE_FEN_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_FEN\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_FEN\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${HAVE_FAM_TRUE}" && test -z "${HAVE_FAM_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"HAVE_FAM\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"HAVE_FAM\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${ENABLE_TIMELOOP_TRUE}" && test -z "${ENABLE_TIMELOOP_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"ENABLE_TIMELOOP\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"ENABLE_TIMELOOP\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${PLATFORM_WIN32_TRUE}" && test -z "${PLATFORM_WIN32_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"PLATFORM_WIN32\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"PLATFORM_WIN32\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${ENABLE_REGEX_TRUE}" && test -z "${ENABLE_REGEX_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"ENABLE_REGEX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"ENABLE_REGEX\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${USE_SYSTEM_PCRE_TRUE}" && test -z "${USE_SYSTEM_PCRE_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"USE_SYSTEM_PCRE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"USE_SYSTEM_PCRE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${USE_SYSTEM_PCRE_TRUE}" && test -z "${USE_SYSTEM_PCRE_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"USE_SYSTEM_PCRE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"USE_SYSTEM_PCRE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${CROSS_COMPILING_TRUE}" && test -z "${CROSS_COMPILING_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"CROSS_COMPILING\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"CROSS_COMPILING\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${ENABLE_GTK_DOC_TRUE}" && test -z "${ENABLE_GTK_DOC_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"ENABLE_GTK_DOC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"ENABLE_GTK_DOC\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${GTK_DOC_BUILD_HTML_TRUE}" && test -z "${GTK_DOC_BUILD_HTML_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"GTK_DOC_BUILD_HTML\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"GTK_DOC_BUILD_HTML\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${GTK_DOC_BUILD_PDF_TRUE}" && test -z "${GTK_DOC_BUILD_PDF_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"GTK_DOC_BUILD_PDF\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"GTK_DOC_BUILD_PDF\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${GTK_DOC_USE_LIBTOOL_TRUE}" && test -z "${GTK_DOC_USE_LIBTOOL_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"GTK_DOC_USE_LIBTOOL\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"GTK_DOC_USE_LIBTOOL\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${GTK_DOC_USE_REBASE_TRUE}" && test -z "${GTK_DOC_USE_REBASE_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"GTK_DOC_USE_REBASE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"GTK_DOC_USE_REBASE\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 if test -z "${ENABLE_MAN_TRUE}" && test -z "${ENABLE_MAN_FALSE}"; then
   { { $as_echo "$as_me:$LINENO: error: conditional \"ENABLE_MAN\" was never defined.
 Usually this means the macro was only invoked conditionally." >&5
 $as_echo "$as_me: error: conditional \"ENABLE_MAN\" was never defined.
 Usually this means the macro was only invoked conditionally." >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi
 
 : ${CONFIG_STATUS=./config.status}
@@ -46751,7 +46751,7 @@
     ' >$as_me.lineno &&
   chmod +x "$as_me.lineno" ||
     { $as_echo "$as_me: error: cannot create $as_me.lineno; rerun with a POSIX shell" >&2
-   { (exit 1); exit 1; }; }
+    }
 
   # Don't try to exec as it changes $[0], causing all sort of problems
   # (the dirname of $[0] is not the place where we might find the
@@ -46978,7 +46978,7 @@
     # Conflict between --help and --header
     { $as_echo "$as_me: error: ambiguous option: $1
 Try \`$0 --help' for more information." >&2
-   { (exit 1); exit 1; }; };;
+    };;
   --help | --hel | -h )
     $as_echo "$ac_cs_usage"; exit ;;
   -q | -quiet | --quiet | --quie | --qui | --qu | --q \
@@ -46988,7 +46988,7 @@
   # This is an error.
   -*) { $as_echo "$as_me: error: unrecognized option: $1
 Try \`$0 --help' for more information." >&2
-   { (exit 1); exit 1; }; } ;;
+    } ;;
 
   *) ac_config_targets="$ac_config_targets $1"
      ac_need_defaults=false ;;
@@ -47811,7 +47811,7 @@
 
   *) { { $as_echo "$as_me:$LINENO: error: invalid argument: $ac_config_target" >&5
 $as_echo "$as_me: error: invalid argument: $ac_config_target" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
   esac
 done
 
@@ -47880,14 +47880,14 @@
 } >conf$$subs.sh ||
   { { $as_echo "$as_me:$LINENO: error: could not make $CONFIG_STATUS" >&5
 $as_echo "$as_me: error: could not make $CONFIG_STATUS" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 ac_delim_num=`echo "$ac_subst_vars" | grep -c '$'`
 ac_delim='%!_!# '
 for ac_last_try in false false false false false :; do
   . ./conf$$subs.sh ||
     { { $as_echo "$as_me:$LINENO: error: could not make $CONFIG_STATUS" >&5
 $as_echo "$as_me: error: could not make $CONFIG_STATUS" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 
   ac_delim_n=`sed -n "s/.*$ac_delim\$/X/p" conf$$subs.awk | grep -c X`
   if test $ac_delim_n = $ac_delim_num; then
@@ -47895,7 +47895,7 @@
   elif $ac_last_try; then
     { { $as_echo "$as_me:$LINENO: error: could not make $CONFIG_STATUS" >&5
 $as_echo "$as_me: error: could not make $CONFIG_STATUS" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   else
     ac_delim="$ac_delim!$ac_delim _$ac_delim!! "
   fi
@@ -47986,7 +47986,7 @@
 fi < "$tmp/subs1.awk" > "$tmp/subs.awk" \
   || { { $as_echo "$as_me:$LINENO: error: could not setup config files machinery" >&5
 $as_echo "$as_me: error: could not setup config files machinery" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 _ACEOF
 
 # VPATH may cause trouble with some makes, so we remove $(srcdir),
@@ -48029,7 +48029,7 @@
   elif $ac_last_try; then
     { { $as_echo "$as_me:$LINENO: error: could not make $CONFIG_HEADERS" >&5
 $as_echo "$as_me: error: could not make $CONFIG_HEADERS" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   else
     ac_delim="$ac_delim!$ac_delim _$ac_delim!! "
   fi
@@ -48116,7 +48116,7 @@
 cat >>$CONFIG_STATUS <<\_ACEOF || ac_write_fail=1
   { { $as_echo "$as_me:$LINENO: error: could not setup config headers machinery" >&5
 $as_echo "$as_me: error: could not setup config headers machinery" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 fi # test -n "$CONFIG_HEADERS"
 
 
@@ -48131,7 +48131,7 @@
   :[FHL]*:*);;
   :L* | :C*:*) { { $as_echo "$as_me:$LINENO: error: invalid tag $ac_tag" >&5
 $as_echo "$as_me: error: invalid tag $ac_tag" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
   :[FH]-) ac_tag=-:-;;
   :[FH]*) ac_tag=$ac_tag:$ac_tag.in;;
   esac
@@ -48161,7 +48161,7 @@
 	   esac ||
 	   { { $as_echo "$as_me:$LINENO: error: cannot find input file: $ac_f" >&5
 $as_echo "$as_me: error: cannot find input file: $ac_f" >&2;}
-   { (exit 1); exit 1; }; };;
+    };;
       esac
       case $ac_f in *\'*) ac_f=`$as_echo "$ac_f" | sed "s/'/'\\\\\\\\''/g"`;; esac
       ac_file_inputs="$ac_file_inputs '$ac_f'"
@@ -48190,7 +48190,7 @@
     *:-:* | *:-) cat >"$tmp/stdin" \
       || { { $as_echo "$as_me:$LINENO: error: could not create $ac_file" >&5
 $as_echo "$as_me: error: could not create $ac_file" >&2;}
-   { (exit 1); exit 1; }; } ;;
+    } ;;
     esac
     ;;
   esac
@@ -48258,7 +48258,7 @@
     test -z "$as_dirs" || eval "mkdir $as_dirs"
   } || test -d "$as_dir" || { { $as_echo "$as_me:$LINENO: error: cannot create directory $as_dir" >&5
 $as_echo "$as_me: error: cannot create directory $as_dir" >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
   ac_builddir=.
 
 case "$ac_dir" in
@@ -48371,7 +48371,7 @@
 eval sed \"\$ac_sed_extra\" "$ac_file_inputs" | $AWK -f "$tmp/subs.awk" >$tmp/out \
   || { { $as_echo "$as_me:$LINENO: error: could not create $ac_file" >&5
 $as_echo "$as_me: error: could not create $ac_file" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 
 test -z "$ac_datarootdir_hack$ac_datarootdir_seen" &&
   { ac_out=`sed -n '/\${datarootdir}/p' "$tmp/out"`; test -n "$ac_out"; } &&
@@ -48388,7 +48388,7 @@
   esac \
   || { { $as_echo "$as_me:$LINENO: error: could not create $ac_file" >&5
 $as_echo "$as_me: error: could not create $ac_file" >&2;}
-   { (exit 1); exit 1; }; }
+    }
  ;;
   :H)
   #
@@ -48401,7 +48401,7 @@
     } >"$tmp/config.h" \
       || { { $as_echo "$as_me:$LINENO: error: could not create $ac_file" >&5
 $as_echo "$as_me: error: could not create $ac_file" >&2;}
-   { (exit 1); exit 1; }; }
+    }
     if diff "$ac_file" "$tmp/config.h" >/dev/null 2>&1; then
       { $as_echo "$as_me:$LINENO: $ac_file is unchanged" >&5
 $as_echo "$as_me: $ac_file is unchanged" >&6;}
@@ -48410,14 +48410,14 @@
       mv "$tmp/config.h" "$ac_file" \
 	|| { { $as_echo "$as_me:$LINENO: error: could not create $ac_file" >&5
 $as_echo "$as_me: error: could not create $ac_file" >&2;}
-   { (exit 1); exit 1; }; }
+    }
     fi
   else
     $as_echo "/* $configure_input  */" \
       && eval '$AWK -f "$tmp/defines.awk"' "$ac_file_inputs" \
       || { { $as_echo "$as_me:$LINENO: error: could not create -" >&5
 $as_echo "$as_me: error: could not create -" >&2;}
-   { (exit 1); exit 1; }; }
+    }
   fi
 # Compute "$ac_file"'s index in $config_headers.
 _am_arg="$ac_file"
@@ -48590,7 +48590,7 @@
     test -z "$as_dirs" || eval "mkdir $as_dirs"
   } || test -d "$as_dir" || { { $as_echo "$as_me:$LINENO: error: cannot create directory $as_dir" >&5
 $as_echo "$as_me: error: cannot create directory $as_dir" >&2;}
-   { (exit 1); exit 1; }; }; }
+    }; }
       # echo "creating $dirpart/$file"
       echo '# dummy' > "$dirpart/$file"
     done
@@ -49798,7 +49798,7 @@
 test $ac_write_fail = 0 ||
   { { $as_echo "$as_me:$LINENO: error: write failure creating $CONFIG_STATUS" >&5
 $as_echo "$as_me: error: write failure creating $CONFIG_STATUS" >&2;}
-   { (exit 1); exit 1; }; }
+    }
 
 
 # configure is writing to config.log, and then calls config.status.
