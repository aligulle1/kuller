Index: lsof_4.81_src/proc.c
===================================================================
--- lsof_4.81_src.orig/proc.c
+++ lsof_4.81_src/proc.c
@@ -841,8 +841,6 @@ link_lfile()
 {
 	if (Lf->sf & SELEXCLF)
 	    return;
-	if (((Selflags & SELNET) == SELNET) && !(Lf->sf & SELNET))
-	    return;
 	Lp->pss |= PS_SEC;
 	if (Plf)
 	    Plf->next = Lf;
