Index: tigervnc-1.0.90/unix/vncserver
===================================================================
--- tigervnc-1.0.90.orig/unix/vncserver
+++ tigervnc-1.0.90/unix/vncserver
@@ -59,22 +59,11 @@ $defaultXStartup
        "vncconfig -iconic &\n".
        "unset SESSION_MANAGER\n".
        "unset DBUS_SESSION_BUS_ADDRESS\n".
-       "OS=`uname -s`\n".
-       "if [ \$OS = 'Linux' ]; then\n".
-       "  case \"\$WINDOWMANAGER\" in\n".
-       "    \*gnome\*)\n".
-       "      if [ -e /etc/SuSE-release ]; then\n".
-       "        PATH=\$PATH:/opt/gnome/bin\n".
-       "        export PATH\n".
-       "      fi\n".
-       "      ;;\n".
-       "  esac\n".
+       "if [ -x /usr/lib/X11/xinit/xinitrc ]; then\n".
+       "  exec /usr/lib/X11/xinit/xinitrc\n".
        "fi\n".
-       "if [ -x /etc/X11/xinit/xinitrc ]; then\n".
-       "  exec /etc/X11/xinit/xinitrc\n".
-       "fi\n".
-       "if [ -f /etc/X11/xinit/xinitrc ]; then\n".
-       "  exec sh /etc/X11/xinit/xinitrc\n".
+       "if [ -f /usr/lib/X11/xinit/xinitrc ]; then\n".
+       "  exec sh /usr/lib/X11/xinit/xinitrc\n".
        "fi\n".
        "[ -r \$HOME/.Xresources ] && xrdb \$HOME/.Xresources\n".
        "xsetroot -solid grey\n".
