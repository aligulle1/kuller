diff -Naur festival.orig/src/arch/festival/festival.cc festival/src/arch/festival/festival.cc
--- festival.orig/src/arch/festival/festival.cc	2005-11-16 00:26:24.000000000 +0200
+++ festival/src/arch/festival/festival.cc	2007-10-25 17:45:06.000000000 +0300
@@ -68,7 +68,7 @@
 #endif
 
 #ifndef FTLIBDIR
-#define FTLIBDIR "/projects/festival/lib/"
+#define FTLIBDIR "/usr/share/festival/lib"
 #endif
 #ifndef FTOSTYPE
 #define FTOSTYPE ""
diff -Naur festival.orig/src/arch/festival/Makefile festival/src/arch/festival/Makefile
--- festival.orig/src/arch/festival/Makefile	2004-01-28 01:32:17.000000000 +0200
+++ festival/src/arch/festival/Makefile	2007-10-25 17:45:52.000000000 +0300
@@ -62,6 +62,6 @@
 LOCAL_INCLUDES += $(FESTIVAL_INCLUDES)
 
 festival.o: festival.cc
-	$(CXX_COMMAND_TEMPLATES) -DFTNAME='$(PROJECT_NAME)' -DFTLIBDIRC='$(FTLIBDIR)' -DFTVERSION='$(PROJECT_VERSION)' -DFTSTATE='$(PROJECT_STATE)'  -DFTDATE='$(PROJECT_DATE)' -DFTOSTYPE=\"$(SYSTEM_TYPE)\" festival.cc
+	$(CXX_COMMAND_TEMPLATES) -DFTNAME='$(PROJECT_NAME)' -DFTLIBDIRC='/usr/share/festival/lib' -DFTVERSION='$(PROJECT_VERSION)' -DFTSTATE='$(PROJECT_STATE)'  -DFTDATE='$(PROJECT_DATE)' -DFTOSTYPE=\"$(SYSTEM_TYPE)\" festival.cc
 
 
diff -Naur festival.orig/lib/Makefile festival/lib/Makefile
--- festival.orig/lib/Makefile	2007-10-25 17:47:53.000000000 +0300
+++ festival/lib/Makefile	2007-10-25 17:59:25.000000000 +0300
@@ -75,8 +75,8 @@
 .copy_from_est: $(SIOD)
 	@:
 
-$(SIOD) : % : $(EST)/lib/siod/%
-	@echo 'Copy $* from EST/lib/siod'
+$(SIOD) : % : $(EST)/siod/%
+	@echo 'Copy $* from EST/siod'
 	@$(RM) -f $*
 	@{ \
 	echo '' ;\
@@ -86,7 +86,7 @@
 	echo ' ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;' ;\
 	echo ' ;;; DO NOT EDIT THIS FILE ON PAIN OF MORE PAIN.' ;\
 	echo ' ;;; ' ;\
-	echo ' ;;; The master copy of this file is in $(EST)/lib/siod/$*' ;\
+	echo ' ;;; The master copy of this file is in $(EST)/siod/$*' ;\
 	echo ' ;;; and is copied here at build time.' ;\
 	echo ' ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;' ;\
 	echo '' ;\
@@ -101,6 +101,6 @@
 	echo '' ;\
 	echo '' ;\
 	echo '' ;\
-	} | cat - $(EST)/lib/siod/$* |sed -e '/mode: *scheme/s//mode: view/' > $*
+	} | cat - $(EST)/siod/$* |sed -e '/mode: *scheme/s//mode: view/' > $*
 	@chmod a-w $*
 
