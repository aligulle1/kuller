diff -Naur audiere/src/speexfile/speexfile.cpp audiere.tpg/src/speexfile/speexfile.cpp
--- audiere-1.9.4/src/speexfile/speexfile.cpp	2004-01-11 03:36:25.000000000 +0000
+++ audiere-1.9.4.tpg/src/speexfile/speexfile.cpp	2007-06-25 19:48:09.000000000 +0000
@@ -889,7 +889,7 @@
     modeID = header->mode;
     if ( forceMode != -1 )
         modeID = forceMode;
-    mode = speex_mode_list[modeID];
+    mode = (SpeexMode*)speex_mode_list[modeID];
 
     if ( mode->bitstream_version < header->mode_bitstream_version ) {
         strcpy ( speex_last_error, "The file was encoded with a newer version of Speex.\nYou need to upgrade in order to play it." );
