Index: libavcodec/dct-test.c
===================================================================
--- libavcodec/dct-test.c	(revision 12559)
+++ libavcodec/dct-test.c	(working copy)
@@ -531,6 +531,7 @@
 
     init_fdct();
     idct_mmx_init();
+    mm_flags = mm_support();
 
     for(i=0;i<256;i++) cropTbl[i + MAX_NEG_CROP] = i;
     for(i=0;i<MAX_NEG_CROP;i++) {
