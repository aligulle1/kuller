--- src/modules/sdl/Makefile.orig	2008-06-16 15:16:51.000000000 +0300
+++ src/modules/sdl/Makefile	2008-06-16 15:17:20.000000000 +0300
@@ -18,10 +18,10 @@
 
 CFLAGS +=-I../.. `sdl-config --cflags`
 
-LDFLAGS +=`sdl-config --libs`
-
 LDFLAGS +=-L../../framework -lmlt
 
+LDFLAGS +=`sdl-config --libs`
+
 ifeq ($(WITH_SDL_IMAGE),1)
 OBJS += producer_sdl_image.o
 CFLAGS += -DWITH_SDL_IMAGE
--- src/modules/sox/Makefile.orig	2008-06-16 15:17:26.000000000 +0300
+++ src/modules/sox/Makefile	2008-06-16 15:20:31.000000000 +0300
@@ -1,5 +1,4 @@
 include ../../../config.mak
-include config.mak
 
 TARGET = ../libmltsox$(LIBSUF)
 
@@ -10,6 +9,8 @@
 
 LDFLAGS += -L../../framework -lmlt
 
+include config.mak
+
 SRCS := $(OBJS:.o=.c)
 
 all: 	$(TARGET)
