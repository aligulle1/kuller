--- system-config-printer-1.1.3/cupshelpers/cupshelpers.py.orig	2009-01-29 17:33:28.000000000 +0200
+++ system-config-printer-1.1.3/cupshelpers/cupshelpers.py	2009-02-18 11:34:37.000000000 +0200
@@ -48,6 +48,8 @@
         return "<cupshelpers.Printer \"%s\">" % self.name
 
     def _expand_flags(self):
+        current_ctype = locale.getlocale(locale.LC_CTYPE)
+        locale.setlocale(locale.LC_CTYPE, "C")
         prefix = "CUPS_PRINTER_"
         prefix_length = len(prefix)
         # loop over cups constants
@@ -60,6 +62,10 @@
                 setattr(self, attr_name,
                         bool(self.type & getattr(cups, name)))
 
+        # Restore LC_CTYPE
+        locale.setlocale(locale.LC_CTYPE, current_ctype)
+
+
     def update(self, **kw):
         """
         Update object from printer attributes.
