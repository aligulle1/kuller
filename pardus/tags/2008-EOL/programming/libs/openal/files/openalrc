;;(define devices '(alsa native))
;;(define devices '(alsa))
;;(define alsa-device "pulse")

;; use sdl for pulse until there is a native pulse backend
(define devices '(sdl))

;; uncomment this to output via the 2nd soundcard
;;(define alsa-device "plughw:2,0")

