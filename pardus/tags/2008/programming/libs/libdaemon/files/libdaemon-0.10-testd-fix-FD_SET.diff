--- examples/testd.c
+++ examples/testd.c
@@ -116,7 +116,8 @@
 
         /* Prepare for select() on the signal fd */
         FD_ZERO(&fds);
-        FD_SET(fd = daemon_signal_fd(), &fds);
+	fd = daemon_signal_fd();
+        FD_SET(fd, &fds);
         
         while (!quit) {
             fd_set fds2 = fds;
